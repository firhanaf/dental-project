# ── Secrets & environment ────────────────────
.env
.env.prod
.env.local
*.env

# ── Go build artifacts ───────────────────────
dental-api/bin/
dental-api/*.exe
dental-api/*.test

# ── File uploads (user data, jangan dicommit) ─
dental-api/uploads/
uploads/

# ── Docker volumes & data ────────────────────
postgres-data/
backups/

# ── Nginx SSL config (jangan commit jika berisi path SSL) ──
nginx/default.ssl.conf

# ── IDE ──────────────────────────────────────
.idea/
.vscode/
*.iml

# ── OS ───────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Claude Code local settings ───────────────
.claude/settings.local.json
